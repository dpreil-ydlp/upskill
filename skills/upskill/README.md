# Upskill

Marketplace-compatible copy of the `upskill` skill.

The validator expects at least one skill file at `skills/<name>/SKILL.md`. This
directory provides that layout while preserving the original repository content.
